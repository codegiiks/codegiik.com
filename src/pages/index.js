import React from 'react'

import Logo from '../components/Logo'

export default () => (
  <div style={{
    height: '100vh',
    width: '100vw',
    display: 'flex',
    alignItems: 'center',
    placeContent: 'center'
  }}>
    <Logo></Logo>
  </div>
)
